/**
 * @trafi/db
 *
 * Prisma client for database access.
 *
 * CRITICAL: This package must ONLY be imported by apps/api.
 * NEVER import this package in apps/dashboard or apps/storefront.
 * ESLint rules enforce this restriction.
 */

// Prisma client will be exported here after schema setup in Story 1.5
export const placeholder = 'Prisma client will be configured in Story 1.5';
